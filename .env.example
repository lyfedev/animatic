# Google Cloud (required from Phase 3 onwards)
GOOGLE_CLOUD_PROJECT=
GOOGLE_APPLICATION_CREDENTIALS=

# AWS (injected by ECS task role in production — only needed locally)
AWS_REGION=us-east-1

# App
ENVIRONMENT=development
