---
phase: 4-panel-generation
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - output/panels/index.json
  - .planning/phases/phase-4/04-ART-REVIEW.md
  - .planning/WINDOWS.md
  - .planning/STATE.md
  - README.md
autonomous: true
requirements: [FR-03, NFR-03, NFR-04]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every one of the 49 beats in scenes 1-8 has a generated panel on disk and in S3 (ROADMAP criterion 1)"
    - "All 49 panels are black line art on white at a consistent line weight, drawn from the same STYLE_BLOCK the slot art uses (ROADMAP criterion 2)"
    - "Wide and medium panels carry no facial features and close-ups carry brow, mouth and nose only, checked on real images outside scene 2 as well (ROADMAP criterion 3, D-05)"
    - "Every index entry records its beat_id, the asset slots it used, its prompt, its shot size and the rule that assigned it (ROADMAP criterion 4, D-04, NFR-04)"
    - "Re-running with unchanged beats and assets reuses every cached panel and makes zero image calls, and replacing one slot's art redraws exactly that slot's beats (ROADMAP criterion 5, and Phase 3's criterion 4 demonstrated live)"
    - "Phase 7 can read the panel contract — index location, per-entry fields, beat order and duration — from STATE.md without re-reading this phase's code"
  artifacts:
    - output/panels/index.json
    - .planning/phases/phase-4/04-ART-REVIEW.md
    - .planning/STATE.md
    - README.md
  key_links:
    - "output/panels/index.json in beat order, each entry carrying duration_secs, is what Phase 7 assembles the cut from"
    - "The replace-and-restore experiment on a real slot file is the evidence that closes Phase 3's ROADMAP criterion 4"
    - "STATE.md's Panel Contract sits alongside the Beat Contract and the Asset Slot Contract — the handoff Phase 5, 6 and 7 read"
---

<objective>
Generate the remaining 30 beats, prove the cache on real files rather than only in tests, and
write down the panel contract the later phases consume.

Purpose: scene 2 proved the look. This plan finishes the cut's still frames and closes the
two caching criteria — this phase's criterion 5 and Phase 3's criterion 4, which was left
open on the promise that Phase 4's panel cache would demonstrate it. A test with a fixture
manifest is not that demonstration; replacing a real slot file and watching exactly the right
panels redraw is.

Output: 49 panels, three captured cache states, and a Panel Contract in STATE.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/phase-4/04-CONTEXT.md
@.planning/phases/phase-4/04-01-SUMMARY.md
@.planning/phases/phase-4/04-02-SUMMARY.md
@.planning/phases/phase-4/04-ART-REVIEW.md

@scripts/build_panels.py
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The full run — all 49 panels, and a look at the ones outside scene 2</name>
  <precondition>The scene-2 decision gate in 04-02 was answered approve or accept-with-note, and GOOGLE_API_KEY answers a live gemini-3.1-flash-image call.</precondition>
  <files>output/panels/, output/panels/index.json, .planning/phases/phase-4/04-ART-REVIEW.md, .planning/WINDOWS.md</files>
  <action>
Run `PYTHONPATH=src python scripts/build_panels.py` with no scene filter. Scene 2's 19 panels
reuse from cache at zero API cost; the other 30 generate at roughly ten seconds each, so
about five minutes (D-10). Sequential calls, one bad panel recorded and skipped rather than
aborting the run.

Then check the numbers against the beat list rather than trusting the run. 49 entries, one
per beat, scenes 1 through 8. Shot sizes split 8 wide, 23 medium and 18 close-up, which is
the D-01 mapping applied to the corpus's 8 establishing, 23 action and 18 dialogue beats.
Every entry has a file on disk, a `panel_s3_uri` or an honest reason it has none, and
non-empty `asset_slots_used`, `prompt`, `shot_size_reason`, `facial_features_reason` and
`source_reason` (ROADMAP criterion 4, NFR-04).

Then look at the pictures from the scenes scene 2 could not exercise. Sample at least four,
chosen to cover what is different outside the fight: `s3b7` (close-up, and the promoter is
the character whose thin scene context is a known carry-forward — D-11 — because his scene
never mentions the sport, so he can read as belonging to the wrong world entirely), `s5b4`
(medium, the street beat that names a shop by its quoted lettering, the exact shape that once
got painted into a frame — D-12), `s8b1` (wide, the apartment interior) and `s4b4` (close-up,
the woman, a second reading of the facial clause on a different face). Score each against the
same six points 04-02 used and append the verdicts to `04-ART-REVIEW.md` under a second-pass
heading.

Anything wrong that survives one `--force` retry of the affected beat_ids goes into
`.planning/WINDOWS.md` and is carried. The bar is the intended house style, not perfection —
Phase 7, the first watchable animatic, is the milestone that matters and the deadline is
2026-09-09.

Any beat left with `source="generation_failed"` gets exactly one retry pass with
`--only <beat_id>`; if it still fails, log it in WINDOWS.md with the recorded exception so a
later phase can see which beat has no picture and why.

`output/` is gitignored — record the S3 URIs and the counts in the summary, and do not commit
the images.
  </action>
  <verify>
    <automated>python3 -c "import json,pathlib; from collections import Counter; i=json.load(open('output/panels/index.json')); b=json.load(open('output/beats.json')); ps=i['panels']; assert len(ps)==49, len(ps); assert {p['beat_id'] for p in ps}=={x['beat_id'] for x in b['beats']}, 'index does not cover the beat list'; failed=[p['beat_id'] for p in ps if p['source']=='generation_failed']; assert not failed, failed; missing=[p['beat_id'] for p in ps if not pathlib.Path(p['panel_uri']).is_file() or pathlib.Path(p['panel_uri']).stat().st_size<10000]; assert not missing, missing; c=Counter(p['shot_size'] for p in ps); assert c=={'medium':23,'close-up':18,'wide':8}, c; assert sorted({p['scene'] for p in ps})==[1,2,3,4,5,6,7,8]; bad=[p['beat_id'] for p in ps if not (p['prompt'] and p['asset_slots_used'] and p['shot_size_reason'] and p['facial_features_reason'] and p['source_reason'])]; assert not bad, bad; print('49 panels ok', c)" && ! grep -rn --include='*.py' -E "^[^#]*\b(openai|anthropic|replicate|langchain|stability_sdk|cohere|mistralai)\b" src/ scripts/ && echo "NFR-03 ok: no non-Google AI SDK reference in src/ or scripts/"</automated>
    <human-check>Open four panels from outside scene 2 (s3b7, s5b4, s8b1, s4b4) alongside two from scene 2. Confirm the line weight and the look match across all six, that no words are drawn into any of them, and that the facial rule holds at each shot size. Reply "approved" or name which panels are wrong.</human-check>
  </verify>
  <done>
All 49 beats have a panel on disk over 10KB with a complete index entry; shot sizes split
8/23/18 across wide/medium/close-up; at least four panels from outside scene 2 have been
opened and scored, with the verdicts appended to 04-ART-REVIEW.md and anything unresolved
logged in WINDOWS.md; and no AI SDK other than `google-genai` appears anywhere in `src/` or
`scripts/`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Prove the cache on real files — the unchanged re-run and the replace-and-restore</name>
  <files>output/panels/index.json, output/assets/manifest.json, .planning/phases/phase-4/04-ART-REVIEW.md</files>
  <action>
Three states, captured, on the real tree — not on fixtures.

**State one: nothing changed.** Re-run `PYTHONPATH=src python scripts/build_panels.py` with
nothing touched. Every one of the 49 entries comes back `source="reused"`, `generated_count`
is 0, and no image call is made. This is ROADMAP criterion 5. Capture the console output.

**State two: one slot's art replaced.** Copy `output/assets/generated/ext_street.jpg`'s bytes
over `output/assets/generated/int_rockys_hallway.jpg` — the same experiment Phase 3 ran, on
the slot with exactly one beat (`s7b1`), so the expected blast radius is unambiguous. Re-run
`PYTHONPATH=src python scripts/build_assets.py` so the manifest picks up the new
`content_hash`, then re-run `build_panels.py`. Exactly one panel regenerates, `s7b1`, and the
other 48 reuse. This is ROADMAP Phase 3 criterion 4 — replacing a slot file and re-running
regenerates the panels that use it — demonstrated live, which is the thing Phase 3 deferred
to this phase.

Note what the cache is keyed on while checking this: the panel's own record of each dependent
slot's `content_hash`, diffed against the manifest as it stands right now. Not the manifest's
`stale_beat_ids`, which is a point-in-time signal computed during a different run for a
different purpose.

**State three: restored.** Put the original `int_rockys_hallway.jpg` bytes back, re-run
`build_assets.py` then `build_panels.py` twice, and confirm the tree settles back to 49
reused and zero calls. A cache that does not settle is a cache that redraws forever.

Append all three states to `04-ART-REVIEW.md` under a caching heading with the actual console
counts, so the evidence is checkable rather than asserted. Leave the tree in state three.
  </action>
  <verify>
    <automated>python3 -c "import json,pathlib; i=json.load(open('output/panels/index.json')); ps=i['panels']; assert len(ps)==49, len(ps); assert i.get('generated_count')==0, i.get('generated_count'); assert i.get('reused_count')==49, i.get('reused_count'); notreused=[p['beat_id'] for p in ps if p['source']!='reused']; assert not notreused, notreused; m=json.load(open('output/assets/manifest.json')); assert m['total_slots']==16, m['total_slots']; assert m['stale_beat_ids']==[], m['stale_beat_ids']; hashes={s['slot_id']: s['content_hash'] for s in m['slots']}; drift=[p['beat_id'] for p in ps for sid,h in p['slot_hashes'] if hashes.get(sid)!=h]; assert not drift, drift; print('cache settled: 49 reused, 0 generated, slot hashes match the live manifest')" && python3 -c "import pathlib; r=pathlib.Path('.planning/phases/phase-4/04-ART-REVIEW.md').read_text(); assert 's7b1' in r and 'int_rockys_hallway' in r, 'replace-and-restore evidence not recorded'; print('caching evidence recorded')"</automated>
  </verify>
  <done>
An unchanged re-run reused all 49 panels and made zero image calls; replacing one slot's art
bytes regenerated exactly `s7b1` and nothing else; restoring the bytes settled the tree back
to fully reused; and all three states with their real counts are recorded in
04-ART-REVIEW.md.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write down the Panel Contract for Phases 5, 6 and 7</name>
  <files>.planning/STATE.md, README.md</files>
  <action>
Add a `## Phase 4 — Panel Contract` section to STATE.md in the same register as the Phase 2
Beat Contract and the Phase 3 Asset Slot Contract, so the later phases consume the panels
instead of re-deriving them. Cover:

- **Where the index lives** — `output/panels/index.json` locally and `panels/index.json` in
  the media bucket, with the panels themselves at `output/panels/<beat_id>.<ext>` and
  `panels/<beat_id>.<ext>`. The index names its beat list source and that list's own
  `generated_at`, plus the asset manifest it was built against, so an index can always be
  tied to the exact inputs it came from.
- **The per-entry field set**, and specifically that entries are in beat order and each
  carries `duration_secs` copied from the beat — that is what Phase 7 cuts on, so it does not
  need to join back to the beat list to assemble.
- **Shot size is derived, not stored.** `establishing` wide, `action` medium, `dialogue`
  close-up (D-01), computed at panel-build time and never written back into `output/beats.json`
  (D-02) — the beats stand as initially rendered and adding a field to them would invalidate
  the asset manifest's own hashes. Every panel records both the size and the rule that
  assigned it (D-04), the same way a beat records `duration_source` — note that field carries
  three values in the live corpus (`model`, `page_budget`, `dialogue_floor`), not two.
- **The facial rule is keyed off shot size** (D-05): wide and medium carry no facial features,
  close-ups carry a brow line, a mouth line and a nose. State the three prompt rules that
  produce it and that were paid for twice now — positive prose, the rule that matters placed
  last, and no object named that is not wanted in the picture (D-06, D-07). Note that guards
  assert on the built string, never on the source file.
- **What the cache key covers** — the beat's own content fields, the derived shot size, every
  dependent slot's current `content_hash` read fresh from the manifest at build time, and
  `PROMPT_TEMPLATE_VERSION`. Say why it does not consume the manifest's `stale_beat_ids`, and
  say that bumping the template version by hand after a wording change is the lever that
  forces a redraw without `--force`.
- **`--scene` and `--only` narrow generation, never the index.** All 49 beats stay in the
  index on every run.
- **Panels are generated from text this phase.** Reference-image conditioning is proven and
  held out deliberately (D-08); a later phase that revisits it adds a second `contents` part
  and nothing else changes.
- **The style constant is shared** — panels import the same `STYLE_BLOCK` as slot art from
  `src/animatic/core/style.py`; do not add a second, per-phase style block.

Update the STATE.md decisions log and the milestone row for Phase 4, and record what the
developer approved or flagged at the scene-2 gate.

In README.md, add `PYTHONPATH=src python scripts/build_panels.py` to the "Running locally"
section next to `build_assets.py`, with a one-line note that `--scene N` generates one scene
at a time, that unchanged beats and unchanged slot art reuse their panels without an API
call, and that replacing a slot's art and re-running redraws exactly the panels that use it.
  </action>
  <verify>
    <automated>grep -q "Panel Contract" .planning/STATE.md && grep -q "build_panels.py" README.md && python3 -c "import pathlib; s=pathlib.Path('.planning/STATE.md').read_text(); sec=s.split('## Phase 4 — Panel Contract')[1]; gaps=[k for k in ('output/panels/index.json','duration_secs','PROMPT_TEMPLATE_VERSION','stale_beat_ids','STYLE_BLOCK','close-up') if k not in sec]; assert not gaps, gaps; print('panel contract covers', len(sec), 'chars')"</automated>
  </verify>
  <done>
STATE.md carries a Panel Contract covering the index location and field set, the derived shot
size and its rule, the facial rule by shot size, the cache key and why it reads the manifest
fresh, the whole-index rule and the shared style constant; the decisions log and milestone row
reflect Phase 4; and README documents the new command and its caching behaviour.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| beat list + asset manifest → prompt text | 30 more beats' prose crosses into an image model, including a beat naming a shop by its quoted lettering |
| process → Google Gemini Developer API | Roughly 30 live image calls plus any retry |
| process → S3 `animatic-media-628818` | 49 panel objects and the index published |
| repository → public GitHub | STATE.md, README.md and the art review are committed to a public repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04 | Tampering | panel cache key | high | mitigate | Task 2's replace-and-restore proves on real files that a slot's content_hash change invalidates exactly its own beats and that the tree settles back; the automated gate also re-checks every entry's recorded slot_hashes against the live manifest |
| T-04-03 | Repudiation | S3 publication result | medium | mitigate | Every panel and the index record the `S3Result`'s ok/error from `s3_writer.put_bytes`; the summary reports actual S3 URIs and counts rather than asserting the writes happened |
| T-04-09 | Repudiation | a beat with no picture | medium | mitigate | A beat still failing after one retry is recorded in the index with its exception and logged in WINDOWS.md, so Phase 7 sees which beat has no panel rather than discovering a hole at assembly time |
| T-04-05 | Information Disclosure | committed artifacts in a public repo | high | mitigate | STATE.md, README.md and the art review carry paths, counts and prose only; confirm no API key, credential or `.env` value appears before committing. `output/` is gitignored |
| T-04-10 | Tampering | temporarily overwritten slot art during Task 2 | medium | mitigate | The replace step copies over one generated file whose original is reproducible from the manifest's recorded prompt; Task 2's third state restores the original bytes and the gate fails unless the tree settles back to 49 reused |
| T-04-08 | Denial of Service | live generation spend | medium | accept | Roughly 30 calls for the full run plus at most one retry pass; cache reuse caps every subsequent run at zero |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install task in this plan; `requirements.txt` is unchanged |
</threat_model>

<verification>
- `output/panels/index.json` holds 49 entries covering every beat_id in `output/beats.json`, each with a real image behind it.
- Shot sizes split 8 wide / 23 medium / 18 close-up, matching the D-01 mapping over the corpus.
- An unchanged re-run reuses all 49 and makes zero calls; the replace-and-restore experiment named exactly `s7b1`.
- `PYTHONPATH=src python3 -m pytest tests/ -q` — full suite green.
- `google-genai` remains the only AI SDK referenced in `src/` and `scripts/` (NFR-03).
- STATE.md and README.md reflect the shipped contract.
</verification>

<success_criteria>
- Every beat in scenes 1-8 has a generated panel (criterion 1).
- The panels are black line art on white at a consistent line weight, drawn from the shared style block (criterion 2).
- Wide and medium panels carry no facial features; close-ups carry brow, mouth and nose only, verified on images from outside scene 2 as well (criterion 3).
- Every panel records its beat_id, asset slots, prompt and reason (criterion 4).
- An unchanged re-run reuses every panel, and a replaced slot redraws exactly its own beats — closing this phase's criterion 5 and Phase 3's criterion 4 (criterion 5).
- Phases 5, 6 and 7 can read the panel contract out of STATE.md.
</success_criteria>

<output>
Create `.planning/phases/phase-4/04-03-SUMMARY.md` when done.
</output>
